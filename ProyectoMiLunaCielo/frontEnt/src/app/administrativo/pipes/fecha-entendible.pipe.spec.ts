import { FechaEntendiblePipe } from './fecha-entendible.pipe';

describe('FechaEntendiblePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaEntendiblePipe();
    expect(pipe).toBeTruthy();
  });
});
